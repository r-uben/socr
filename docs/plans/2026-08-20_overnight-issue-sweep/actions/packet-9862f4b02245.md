# Review packet — action 9862f4b02245

- **Issue**: #127 — https://github.com/r-uben/socr/issues/127
- **Kind**: **COMMENT**  (issue STAYS OPEN; comment only)
- **Disposition**: ROADMAP-UMBRELLA  (SPLIT)
- **Adjudicator (may NOT review this)**: grok  ·  batch batch-2
- **Triage seats for this batch (may NOT review this)**: ollama-deepseek, gemini-flash

> Note: the triage seats SPLIT on this issue. Read both readings before voting.

## Per-seat triage verdicts (machine-checked)

| seat | verdict | evidence_verified | fixed_by_commit |
|---|---|---|---|
| gemini-flash | ROADMAP-UMBRELLA | yes | — |
| ollama-deepseek | FIX-CANDIDATE | yes | — |

## Basis given by the adjudicator

The native collapse is real (1553; no get_links). Deepseek is category-wrong that four features plus golden regeneration is one night, and wrong that heading recovery is a native-only patch — #223 falsifies that half of #127's framing. Gemini's ROADMAP-UMBRELLA matches the issue as written. Native lists/links/emphasis stay in #127's scope; headings do not.

## Risk if this is wrong

A later agent treats the whole native-structure programme as closed or, conversely, ships a heading-only native patch and calls #223 done.

## Evidence cited (already resolved against main_sha by bin/verify_citations.py)

[
 {
  "path": "src/socr/core/born_digital.py",
  "line": 1553,
  "snippet": "line_text = \"\".join(s.get(\"text\", \"\") for s in spans)"
 },
 {
  "path": "src/socr/core/born_digital.py",
  "line": 307,
  "snippet": "GH-127. The native lane ships ``page.get_text(\"text\")``, which is a linear dump:"
 }
]

## EXACT text proposed for posting

        Overnight adjudication at 53b0637: extract_structured still collapses spans to bare text (born_digital.py:1553) and never calls page.get_links() — that native drop is real. #223 is right that the heading half is not native-lane-specific; the contrast 'VLM pages emit real Markdown headings' does not hold. Lists/links/emphasis remain this issue's native-lane scope; headings are the pipeline-wide #223 question and must not be folded into a native-only patch. Not a one-night fix (four features + golden regeneration). ROADMAP-UMBRELLA; stays OPEN.
