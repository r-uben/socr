I'll read the draft ticket graph and the files those tickets would actually touch, then attack it on gating, collisions, and unattended-run failure modes.Next I’ll map the listed issues onto the real files and functions on `main`, then look for missing gates and same-wave collisions.The lane split looks optimistic. I’m checking the actual functions those issues would edit.1. **E (wave 4) — tickets do not exist at dispatch time.** The graph ships a template (`TICKET-E<n> · depends-on: C, lane predecessor`) and defers authoring until after C. No unattended runner can instantiate ids, lane order, or file sets at 03:00. Wave 4 never starts; F1 is blocked on E; morning has tracker comments and zero proposed PRs. **Min fix:** pre-author every candidate now (`E-O1`…`E-F1`) with a skip rule `verdict != STILL-VALID → DONE/skipped`, and add `E0` that only validates the skip set — never invents new E tickets overnight.

2. **E-R #161 × Lane O × Lane F #225 — same-wave disk+semantic collision on the two functions that decide what ships and what resume trusts.** Lanes do not isolate these files.
   - `#161` writes `orchestrator.py::_phase_agentic` (`att.output.audit_passed = att.accepted` ~2762) and `orchestrator.py::_load_terminal_page` (~4723, SUCCESS without `audit_passed`) and `manifest.py::_winning_page_output` (~314–335).
   - Lane O writes the same `_phase_agentic` for halt (~2921–2948), `chart_table_arbitration` (~2267–2297), `_render_chart_region_pngs` (only on `not decision.accepted` ~2864–2878), and TR-3 consumption (~2648, ~2880, ~5008).
   - `#205` writes the TR-3 conjunction in `manifest.py::_winning_page_output` (~363–395) — the same function as #161.
   - `#225` cannot be “figures-only”: `normalizer.py::strip_phantom_images` (~227) **keeps** `http://`/`https://`/`data:`; fail-closing a fabricated URL requires `orchestrator.py::_phase_assemble` / `_rewrite_all_fragments` (~1668, ~5210) and a page-status write next to the other `audit_passed` assignments in `_phase_agentic`.
   Parallel PRs from `origin/main` will conflict inside one 900-line function and can reintroduce the #250 class of bug (fix A undoes fix B’s accept-gate). **Min fix:** one serial owner for `{orchestrator.py, manifest.py, normalizer.py}` for the whole night. Collapse O+R+F (and any T ticket that surfaces status) into that owner. Q2: **serialization of “Lane O” is not sufficient; one owning agent is required.**

3. **E-O “#144/#205 TR-3-on-native-lane” × E-T #198 — missing `depends-on`, cardinal-rule landmine.** `#205` itself forbids keying status/routing on TR-3 until the 62-page set is hand-judged (step 2); step 3 is blocked on that. The draft’s “fix” is step 3. `#205` also states the 25.3% rate shares `is_numeric_token` notation gaps with the sibling `#198` is fixing (`native_verifier.py::is_numeric_token`, `reconstruct.py::_destroyed_numeric_tokens`). Same wave, different lanes, no edge. Merge `#205` first → D3 floor on ~1/4 of native table pages, many of them notation false positives — silent corpus deletion of whole tables. Merge both without order → two PRs retune the same predicate from opposite ends. `#144`’s remaining defect is still `reconstruct.py::rowize_from_words_chart_aware` / `born_digital.py::extract_structured`, not an orchestrator surfacing change; stuffing it into Lane O is a category error. **Min fix:** split `#205` into overnight AuditEvent-only (step 1) vs deferred step 3; make any load-bearing TR-3 ticket `depends-on: #198` **and** a measurement ticket that cannot run unattended. Move actual `#144` rowizer work to Lane T (or the single tables+status owner).

4. **Lane O chart bundle `#249/#248/#189` × Lane T `#195/#197/#198` — reconstruct.py + born_digital.py, not orchestrator-only.** `#248` is `reconstruct.py::has_numeric_columns` (~432) called from `born_digital.py::_detect_tables` (~1259). `#195` threads a reject signal out of `reconstruct.py::reconstruct_table_regions` (~165–182) through `born_digital.py::extract_structured` into page/document/CLI status, and the issue text already names `orchestrator.py` as a possible write. `#197`/`#198` edit the same `_destroyed_numeric_tokens` call site (~165). `#249` reads that `has_tables` bit via `orchestrator.py::_page_has_tables` (~1352) / `chart_table_arbitration`. `#198` widening `_NUM_TOKEN_RE`/`is_numeric_token` changes `has_numeric_columns`’s input set (same regex at reconstruct.py:445) and can manufacture more phantom tables, which is `#249`. Lanes O and T both write `reconstruct.py` and `born_digital.py` in wave 4. **Min fix:** `#248` leaves Lane O and serializes with `#195/#197/#198` on `{reconstruct.py, born_digital.py}`; `#249`/`#189` stay with the orchestrator owner and `depends-on` the `#248` predicate change.

5. **Lane O cascade trio `#221+#222+#227` — missing `depends-on: #159`; writes files Lane T/F do not own.** `#227` states the timeout stub at `agentic.py:232–243` builds `ProviderAttempt` without `provider_id`/`model`/`backend` (budget path ~196–207 populates them). Per-backend canary cannot be selected until `#159` (`agentic.py::route_page` + `orchestrator.py::_phase_agentic.run_provider`). `#221/#222` also change `tables/extract.py::probe_ollama_idle` (~77) and the crop-timeout caller (~299–300). Implement the trio as specified and the night either (a) ships a halt that still cannot see backend identity, or (b) silently expands into `agentic.py`/`extract.py` and collides with any later STILL-VALID `#166`/`#172`. **Min fix:** add `#159` as an explicit predecessor (same owner as orchestrator); keep `probe_ollama_idle` in that same serial owner; do not treat extract.py as Lane T.

6. **`depends-on: C, lane predecessor` deadlocks on skip.** If O1/`#205` is ALREADY-FIXED or NEEDS-MEASUREMENT, O2/O3 wait forever on a predecessor that never becomes DONE. Same inside T (`#195→#197→#198`) and for F1 (`depends-on: D,E`). Unattended: one early skip or red CI freezes the rest of the lane and the morning report. **Min fix:** predecessor edge is `predecessor ∈ {DONE, SKIPPED}`; F1 depends on “every E ticket terminal”, not “every E PR green”. Write the report from whatever finished.

7. **B1–B4 Done when is a hang + a cheat.** Clause: *“every issue in the batch has a verdict from all three vendors, and every verdict carries at least one `path:line` citation.”* One dead vendor CLI (or one hung `gh`/`origin/main` fetch) blocks that batch’s C, then all of D and E. No degrade timeout. Cheat: cite `orchestrator.py:1` (`from __future__` / import) on every issue and emit STILL-VALID. **Min fix:** 2 vendors + escalate on disagreement (open Q1); wall-clock per batch; C treats a missing third as a split, not a blocker. Citation rule: cited line must be a substring of the issue’s claimed symbol/function, checked by C mechanically.

8. **C Done when is a boolean the adjudicator can set without doing the work.** Clause: *“every issue carries a final verdict plus an `evidence_verified` boolean.”* Set `evidence_verified: true` on unchecked quotes; 2–1 splits get recorded and then ignored. Motivated default under uncertainty is NEEDS-MEASUREMENT for the whole batch → D1/D2/D3 vacuously succeed, every E ticket skips, F1 reports a clean night with nothing done. **Min fix:** C must re-read `git show origin/main:<path>` and hash-match the quoted line; `evidence_verified` is computed by that checker, not written by the adjudicator. F1 fails the night if STILL-VALID ∪ ALREADY-FIXED ∪ MISREPORTED is empty and the open set was non-empty.

9. **A3/A4 write the repo; A1 does not gate them; hard fact 2 is violated.** `CONTRACT.md` and `batches.json` are paths under `docs/plans/…`. A3 `depends-on: none`; A4 `depends-on: A2` only. Wave 0 header says serial; the edges say A1/A2/A3 in parallel. They will write the other session’s checkout (`docs/163-reachability-evidence`). **Min fix:** A3 and A4 `depends-on: A1`; all plan artifacts live only in the A1 worktree; pin `origin/main` SHA in A1 and force every later worktree/PR to that SHA (the other session can push main overnight).

10. **A2 abort rule is not an edge.** A2 writes a log line. D1/D2/D3 `depends-on: C` only. Auth death after C → tracker writes become the silent no-ops A2 exists to prevent (or fabricated `gh` success). E also needs the contract (ruff pin, PYTHONPATH, no-merge) and does not depend on A1/A3. **Min fix:** D* `depends-on: A2,C`; E* `depends-on: A1,A3,C`; every D/E prompt starts with a live `gh auth status` check that flips an abort latch D/E honor.

11. **D1/D2/D3 are not tickets a dispatcher can run.** `depends-on: C` is not an id (`C1`–`C4`). D1 and D3 have no Problem/Do/Files. Done when *“each closed issue carries a comment naming the commit and the `file:line` that fixed it”* is vacuous on a zero-close night and is satisfiable by citing an unrelated merge (`#243`/`#246`/`#247`/`#250`) that happens to touch the same file. Three parallel tracker writers share a rate limit; a 403 mid-batch leaves a partial-close set with no rollback. **Min fix:** `D1 depends-on: C1,C2,C3,C4` (or four per-batch D tickets); require the cited commit to actually change the cited symbol (`git log -L` / blame); serialize tracker writes; treat HTTP 403 as abort, not success.

12. **Lane T is not “small, independent.”** `#197` and `#198` both edit `reconstruct.py::_destroyed_numeric_tokens`; `#198` adds `from native_verifier import is_numeric_token` into a module `native_verifier` already imports `_NUM_TOKEN_RE` from (circular import, called out at reconstruct.py:545). Same-wave parallel T is a merge conflict plus an import cycle. “Lane predecessor” is not a ticket id. **Min fix:** `E-T2 #198 depends-on: E-T1 #197`; one agent, one PR for `#197+#198` unless the cycle is resolved first.

13. **Lane O PRs are unspecified as a stack.** Three serial tickets, each “one branch + one PR” from `origin/main`, all touching `_phase_agentic`. Morning gets three unmergeable PRs. **Min fix:** O (or the single orchestrator owner) is one branch / one PR, or a stacked series where O(n) is based on O(n-1), never on `main`.

14. **E Done when non-vacuity is forgeable.** Clause: *“the diff has a regression test that FAILS with the fix reverted (non-vacuity stated in the PR).”* Cheat: test a helper that exists only in the fix (`def _new_gate` removed → ImportError), or an ollama-backed test that CI never runs, or a test that does not patch `_available_engines_for_agentic` (hard fact 3) so the agent “fixes” CI by deleting the assertion. **Min fix:** revert-test must fail on a named fixture/page assertion that already exists on `main` (or is added in the same PR against current `main` behavior); CI-visible; agentic tests must patch `_available_engines_for_agentic`; reviewer is a different model and re-runs the revert locally with `PYTHONPATH=<wt>/src`.

15. **A1/A4 Done when are locally forgeable and go stale.** A1: *“sentinel demonstration is recorded in the log”* — write the sentence, skip the break/restore; later E worktrees never re-prove isolation and can test the main checkout. A4: *“file's issue numbers … exactly equal the live open set”* — true at T0; D1 closes and D3 files overnight, so the morning re-check fails and batch coverage is un-auditable. **Min fix:** A1 writes a canary script E* must run (exit 1 if `socr.__file__` is not under the worktree). A4 is a snapshot with `as_of` SHA + issue-updated-at; F1 diffs live vs snapshot instead of requiring equality.

**Sound, do not pad:** B→C “unanimous acts, split escalates, bad evidence demoted to NEEDS-MEASUREMENT” is the right adjudication shape once the boolean is computed, not self-attested. D2’s “correct in place, stay open” matches `#249`’s actual history. Bundling `#221+#222+#227` as one change is the one Lane O grouping that is correct (same 4-line predicate).
