# 2026-09-06 — advisory panel on the draft ticket graph

Seats: GPT-5.6-sol (gating/size), DeepSeek-v4-flash (merge-conflict/correctness), Kimi K3
(coverage). Gemini seat failed (antigravity transport cannot use caller tools); Grok seat
failed (quota exhausted). Findings verified in code before adoption:

- `structure_class_grid_winner` is pure over `PageState`, which carries no words → A1 split
  into A1a/A1b/A1c; words stored at extraction (DeepSeek 1, GPT 1–2).
- `bind()` uses multiset anchors + interpolation, not ordered runs; `parse_grid` first table
  only; `_assign_bands` round(y0) = #600 → new `corroborate_rows()` (GPT 2, Kimi 1).
- WARNING is non-terminal on resume (`_load_terminal_page`) → A1c states the contract (DeepSeek 4).
- Floor ending already splices prose behind the GH-520 four-condition guard
  (`manifest.py:938`) → B1 rescoped to the `page_failed` ending (GPT 12, DeepSeek 7–8).
- Prose pages return `get_text("text")` before the block walk (`born_digital.py:2195`) →
  C1 seam moved; equal-run-length guard replaced by a gutter/word-space ratio (GPT 9–10).
- Wave collisions on `manifest.py` (A2/B1) and `orchestrator.py` (D2/E1) → serialised (GPT 6–7, Kimi 2).
- Precision guard (fabricated extras), zero-numeric abstain, share-not-count constant,
  A1→A2 window closed by a row-count floor in A1b (Kimi 5–7, DeepSeek 2, 11).
- `chart_pages` key absent on main → E1 done-when rewritten (Kimi 8).
- Normalizer returns only text → F1 split into F1a/F1b (GPT 15).
- Added D3 (Fed table-lane re-measure on main) and explicit out-of-scope list
  (#215, #270, #393, #249, #263, hyphenation) (Kimi 3–4, 13, 15).
Not adopted: DeepSeek 3 (replace header with `col N` stubs) — kept as an owner option, header
text recorded in the sidecar so it stays reversible.
