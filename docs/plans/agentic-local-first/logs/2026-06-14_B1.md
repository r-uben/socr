# TICKET-B1 — Drop DeepSeek / demote Mistral from the agentic ladder

Date: 2026-06-14
Status: DONE

## What changed

This is a thin ticket: A1 already did the core work. B1 adds explicit verification tests
and confirms the existing implementation is correct.

### Verified (no code changes needed)

- `PROFILE_MISTRAL.auto_eligible = False` in `src/socr/core/providers.py` (line 127)
- `PROFILE_DEEPSEEK.auto_eligible = False` in `src/socr/core/providers.py` (line 137)
- `provider_ladder()` filters by `auto_eligible` via the `include_ineligible` param (A1)
- CLI `ENGINE_CHOICES` in `src/socr/cli.py` includes both `deepseek` and `mistral`,
  so `--primary deepseek` and `--primary mistral` remain valid for explicit override
- `cost_of()` uses `DEFAULT_PROVIDERS` directly (not filtered) so replay works
- `provider_ladder(None, include_ineligible=True)` still returns both profiles

### Added tests in `tests/test_providers.py`

Four new B1-specific tests using `provider_ladder(None, ...)` (all-providers path):

1. `test_default_ladder_excludes_deepseek_and_mistral` — confirms `provider_ladder(None)`
   excludes both DEEPSEEK and MISTRAL from the automatic ladder
2. `test_include_ineligible_includes_deepseek_and_mistral` — confirms
   `provider_ladder(None, include_ineligible=True)` brings both back
3. `test_cost_of_deepseek_still_works` — `cost_of(EngineType.DEEPSEEK, 5) == 0.0`
4. `test_cost_of_mistral_still_works` — `cost_of(EngineType.MISTRAL, 5) == 0.001 * 5`

## Files changed

- `tests/test_providers.py` — 4 tests added (lines 146-174)
- `docs/plans/agentic-local-first/STATUS.md` — B1 flipped to DONE
- `docs/plans/agentic-local-first/logs/2026-06-14_B1.md` — this file

## Test results

- `tests/test_providers.py`: 16 passed (12 pre-existing + 4 new)
- Full suite: 720 passed

## Deviations / follow-up

None. The worktree needed a `git merge feat/46-model-lineup-refresh` to pick up A1
commits before work could start (worktree was created before A1 landed).
