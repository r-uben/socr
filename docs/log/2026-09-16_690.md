# 2026-09-16 — GH-690: spacer filter drops unlabeled rows with entity-encoded values

Worked in `/Users/rubenffuertes/repos/.worktrees/socr-690` (branch
`fix/690-spacer-entity-numbers`, based on `origin/main@7d4b5ef`). All commands run with
`PYTHONPATH=/Users/rubenffuertes/repos/.worktrees/socr-690/src`; `socr.__file__` confirmed
pointing inside the worktree before any test run.

## Defect confirmed (team lead's measurement, reproduced)

`_candidate_row_multiset` (`src/socr/tables/binding.py`) ran `is_numeric_token(tok)` on the
RAW cell. `_normalize_candidate_rows` only HTML-unescapes the LABEL cell
(`_normalize_label_cell`, col 0). An unlabeled row whose only value is entity-encoded
(`&minus;1.5`) or `&nbsp;`-prefixed (`&nbsp;62.5`) was therefore misclassified as a #601
layout spacer by `_is_spacer_row`, and `bind()` silently dropped it.

`_candidate_row_multiset` is the SAME function `_bind_rows` calls for the row-anchoring
numeric multiset compare, so the hole was not confined to spacer classification: an
entity-encoded value row that survived classification for some other reason would still fail
to anchor.

## (a)/(b) choice: promoted `_normalize_cell` to `native_verifier.py`

GH-679 (`6226e92`, merged an hour before this ticket started) added `_normalize_cell`
(`html.unescape` + trailing-dash-rule strip over a named `_TRAILING_DASH_CHARS` constant) to
`src/socr/tables/source_evidence.py`. It is the exact normaliser both tickets want shared, not
copied a third time.

Chose **(b): promote to `src/socr/tables/native_verifier.py`**, not (a) import it across
modules as private to `source_evidence.py`. Reasoning:

- `binding.py` already imports from `native_verifier` (`is_numeric_token`,
  `_normalize_numeric_token`, `label_key`, etc.) — no new cross-module dependency edge.
- `source_evidence.py` already imports from `native_verifier`
  (`_numeric_multiset_from_tokens`, `is_numeric_token`) — same story.
- `binding.py` and `source_evidence.py` do not otherwise import each other; importing (a)
  `_normalize_cell` from `source_evidence` into `binding.py` would have created a NEW,
  otherwise-unjustified dependency edge between two independent table-verification modules for
  the sake of one shared helper. `native_verifier.py` is already the shared low-level
  normalisation module both depend on (`is_numeric_token`, `strip_presentation`,
  `_normalize_numeric_token` all live there) — `_normalize_cell` belongs next to them.
- There is existing precedent for private (`_`-prefixed) helpers in `native_verifier.py` being
  imported across `tables/` submodules (`_normalize_numeric_token`, `_numeric_multiset_from_tokens`),
  so keeping the name `_normalize_cell` (not renaming to a public name) matches the module's
  existing convention rather than introducing a new one.

`source_evidence.py`'s own behaviour must not change: its import was rewritten to
`from socr.tables.native_verifier import (_normalize_cell, _numeric_multiset_from_tokens,
is_numeric_token)`, the local definition (and now-orphaned `_TRAILING_DASH_CHARS`) deleted, no
other line touched (`html` stays imported there — it is used elsewhere in the module, not only
by `_normalize_cell`). GH-679's own test file
(`tests/test_gh679_decorated_numeric_tokens.py`) exercises only the module's PUBLIC functions
(`collect_table_tokens`, `verify_table_tokens`), never `_normalize_cell` directly, so it needed
no changes and is included in the 748-test targeted run below, all passing.

## The fix

`_candidate_row_multiset` now runs `_normalize_cell(cell).strip()` (decode entities, strip
trailing dash-rule decoration) before splitting on whitespace and checking `is_numeric_token`,
instead of the raw `cell.strip()`. One-line change to the function both `_is_spacer_row` (#601
classification) and `_bind_rows` (row-anchoring multiset compare) call — closes both call
sites in one place, matching the ticket's "before spacer classification (and ideally before
multiset compare)" ask.

## Wider `&nbsp;` finding

Confirmed the ticket's own scope note: the hole is not `&minus;`-specific. `&nbsp;62.5` fails
`is_numeric_token` on the raw cell by the identical mechanism (no entity decode before the
numeric-token check) and was dropped the same way. Fixed by the same one-line change (the
normaliser decodes ALL HTML entities via `html.unescape`, not a `&minus;`-specific string
replace) and pinned explicitly in `test_gh690_nbsp_prefixed_value_row_is_kept_not_dropped`.

## Genuine-spacer guarantee — measured both directions

Before writing the fix, confirmed with the SAME normaliser applied:

| row | `_is_spacer_row` | `_candidate_row_multiset` |
| --- | --- | --- |
| `('', '&minus;1.5')` | `False` (was `True`, now correctly kept) | `{'-1.5': 1}` |
| `('', '&nbsp;62.5')` | `False` (was `True`, now correctly kept) | `{'62.5': 1}` |
| `('', '')` | `True` (unchanged) | `{}` |
| `('', '---')` | `True` (unchanged — `_normalize_cell` strips the trailing dash run to `''`) | `{}` |
| `('', '***')` | `True` (unchanged) | `{}` |

No previously-dropped genuine layout row (empty, markdown rule, punctuation-only) is now kept.
All five pinned in `tests/test_gh690_spacer_entity_numbers.py`.

## Mutation proof (load-bearing)

Copied `src/` and `tests/` to `/tmp/socr690_mutant/`, confirmed `socr.__file__` points inside
the copy (`import socr; assert socr.__file__.startswith('/tmp/socr690_mutant')`), asserted the
fix anchor (`for tok in re.split(r"\s+", _normalize_cell(cell).strip()):`) occurred exactly
once in `binding.py` before editing, then reverted the fix in the mutant copy only (back to
`cell.strip()`, no `_normalize_cell` call) and re-ran the new test file:

- `test_gh690_html_minus_entity_value_row_is_kept_not_dropped` — **FAILS** on the mutant
  (`candidate_spacer_rows_dropped` no longer 0).
- `test_gh690_nbsp_prefixed_value_row_is_kept_not_dropped` — **FAILS** on the mutant.
- `test_gh690_candidate_row_multiset_decodes_entity_before_numeric_check` — **FAILS** on the
  mutant (`Counter()` instead of `Counter({'-1.5': 1})` / `Counter({'62.5': 1})`).
- The four genuine-spacer control tests — correctly **PASS** on the mutant (unaffected by the
  fix either way, as they should be).

3 of 7 tests fail exactly as expected when the fix is removed; the 4 controls are unaffected in
both directions. Guard proven load-bearing. Mutant copy deleted after the check
(`/tmp/socr690_mutant`).

## Pinned at `bind()`, not an isolated helper

Per the ticket's instruction, the tests assert `parse_grid(...).spacer_row_indices`,
`.spacer_rows_dropped`, and `bind(...).candidate_spacer_rows_dropped` — the exact counters
`bind()` itself uses internally to drop rows (`if grid.spacer_row_indices: grid =
replace(...)` in `bind()`) — plus a direct unit pin on `_candidate_row_multiset` /
`_is_spacer_row` for the underlying mechanism. Not pinned against an isolated unescape helper.

## Related finding NOT fixed (reported, out of ticket scope)

While reproducing the fixture with a matched native word, found a THIRD instance of the same
underlying gap in a DIFFERENT gate: the per-cell value comparison in `bind()`'s equal-lane-count
walk (`model_value = model_raw.strip() or None`, around `binding.py:2211`) and its salvage-DP
counterpart (`_record_inventions_on_parent_row`, `~binding.py:1643`) also read the RAW candidate
cell, not through `_normalize_cell`. Feeding a native word `-1.5` against candidate
`&minus;1.5` (after this ticket's fix, so the row is no longer dropped as a spacer) produces a
`ContradictedCell` — `is_numeric_token('&minus;1.5')` is `False` on the raw token, so the
mismatch branch fires even though the two values are the same number. This is a false-positive
contradiction, not a silent drop, and it is a DIFFERENT gate than either #679 (evidence-gate
numeric multiset) or this ticket's #601 spacer classifier. Explicitly out of this ticket's
stated scope ("before spacer classification (and ideally before multiset compare)" — the
multiset compare it names is `_candidate_row_multiset` via `_bind_rows`, not the per-cell
match/contradict walk). Not fixed here; flagged for the team lead to file/triage separately —
it has a materially wider blast radius (every table's cell-level match/contradict path, not
just spacer classification) and deserves its own scoped ticket and review, not a
scope-creeping addition to this one.

## Verification

- `tests/test_binding.py tests/test_gh690_spacer_entity_numbers.py
  tests/test_gh679_decorated_numeric_tokens.py` — 106 passed, 1 xfailed.
- Broader targeted run (every test file touching `binding.py` / `native_verifier.py` /
  `source_evidence.py`, 23 files) — 748 passed, 2 xfailed.
- Full suite: run once, coordinated with the team lead to avoid colliding with a concurrent
  read-only designer session on the same machine.
- `uvx ruff@0.16.0 format --check .` — clean (the CI gate, not the venv's older ruff).

## Files changed

- `src/socr/tables/native_verifier.py` — `_normalize_cell` + `_TRAILING_DASH_CHARS` promoted
  here (definition unchanged from `source_evidence.py`'s #679 version, docstring extended with
  the #690 cross-reference).
- `src/socr/tables/source_evidence.py` — local `_normalize_cell` / `_TRAILING_DASH_CHARS`
  deleted, now imports `_normalize_cell` from `native_verifier`.
- `src/socr/tables/binding.py` — `_candidate_row_multiset` normalises each value cell through
  `_normalize_cell` before the numeric-token check.
- `tests/test_gh690_spacer_entity_numbers.py` — new, 7 tests (3 regression, 4 genuine-spacer
  controls).
