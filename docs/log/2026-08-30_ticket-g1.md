# 2026-08-30 — TICKET-G1: flag, config, fingerprint prep (GH-353)

## What changed

Added the config surface for the table judge ladder — a default-off flag plus
every knob the ladder needs, so later tickets (A2/A3/B1/H1) can inject dummy
rung identities in tests instead of reading bare constants.

`PipelineConfig` (`src/socr/core/config.py`):
- `table_judge_ladder: bool = False`
- `table_judge_rung1_model: str = "glm-5.3-flash:cloud"` (the GH-356 bake-off
  winner)
- `table_judge_rung1_host: str | None = None` — resolves the same way
  `ollama_host` does (env var, then localhost default); NOT hardcoded
  localhost.
- `table_judge_rung2_binary: str = "gemini"`
- `table_judge_timeout_sec: float = TABLE_JUDGE_TIMEOUT_SEC_DEFAULT` (600.0) —
  a new named module constant, not a bare literal, documented with the
  bake-off measurement it comes from: glm-5.3-flash exceeded a 300s cap on a
  dense grid and was still *correct* on retry at a 590s cap
  (`docs/log/2026-08-30_gh356-bakeoff.md`), whose explicit follow-up reads
  "timeout >= 600 s". A timeout is documented as ¬S1
  (escalate-as-substitute), never a FAIL, per the ladder design.

Also documented (not implemented — B1's job) the `--strict-local` + ladder
interaction: both rungs are cloud, so the combination makes every rung
unavailable before the first call; the gate must fail-open to UNVERIFIED,
never a silent PASS or an exception.

CLI (`src/socr/cli.py`): added `--table-judge-ladder` (is_flag) to
`common_options` and a matching `table_judge_ladder: bool = False` parameter
on `build_config`. Set with the same **positive-only** pattern as
`strict_local` (`if table_judge_ladder: config.table_judge_ladder = True`) —
NOT the unconditional-override pattern used a few lines above for
`judge_backend`/`judge_model`/`max_cost_per_page`/`cost_budget`/
`write_manifest` (those clobber a loaded YAML config because their Click
options carry non-None defaults of their own; `is_flag` options default to
`False`, which is indistinguishable from "not passed" unless the setter stays
positive-only). Verified: a YAML config with `table_judge_ladder: true` and no
CLI flag survives `build_config`.

Fingerprint extras (flag identity, both rung model/binary, timeout, prompt
digest) are explicitly left for TICKET-B1 per the ticket's scope.

## Files

- `src/socr/core/config.py` — new module constant
  `TABLE_JUDGE_TIMEOUT_SEC_DEFAULT` + five new `PipelineConfig` fields. The
  generic `from_file` sweep (`#240`) restores all five without any change to
  `_FROM_FILE_EXPLICIT_FIELDS` — they are scalars (bool/str/str|None/float),
  not enums/lists/Path.
- `src/socr/cli.py` — `--table-judge-ladder` option in `common_options`,
  parameter + positive-only setter in `build_config`.
- `tests/test_ladder_config.py` (new) — defaults, `--help` listing on both
  `process` and `batch`, the positive-only-override guard (YAML `True`
  surviving an absent flag; CLI flag winning over a YAML `False`), and an
  explicit five-field YAML round-trip alongside the generic sweep already in
  `test_config_from_file.py`.
- `tests/test_cli_flag_agentic_status_gh142.py` — classified the five new
  fields under `_INERT_AND_UNFINGERPRINTED` (neither read nor fingerprinted
  yet; B1 is the ticket that wires the gate and the fingerprint extras). This
  file's own guard (`test_every_config_field_is_classified`) fails CI on any
  unclassified `PipelineConfig` field, so this edit was required, not
  optional, despite not being in G1's listed file ownership.

## Tests

- `~/venvs/socr/bin/pytest tests/test_ladder_config.py -q` → 12 passed.
- `~/venvs/socr/bin/pytest tests/test_config_from_file.py tests/test_r174b_cli_guards.py tests/test_cli_flag_agentic_status_gh142.py -q`
  → 76 passed.
- `~/venvs/socr/bin/pytest tests/test_gh139_no_audit_rejected.py tests/test_r174b_config_schema.py -q`
  → 17 passed.
- `uvx ruff@0.16.0 format --check .` (whole repo) → 393 files already
  formatted, clean.

## Deviations / follow-ups

- Touched `tests/test_cli_flag_agentic_status_gh142.py`, one file outside
  G1's listed ownership (`src/socr/core/config.py`, `src/socr/cli.py`,
  `tests/test_ladder_config.py`), because that file's own field-classification
  guard fails CI the moment a new `PipelineConfig` field exists unclassified.
  This is mechanical bookkeeping (adding five names to an existing
  `_INERT_AND_UNFINGERPRINTED` set with a comment), not a design decision, and
  does not touch any file another wave-1 agent (A0/A1/C1) owns.
- No CLI flags added for `table_judge_rung1_model` /
  `table_judge_rung1_host` / `table_judge_rung2_binary` /
  `table_judge_timeout_sec` — the ticket's `Do` names only
  `--table-judge-ladder` as a CLI flag; the other four are YAML-only knobs by
  design (test/H1 injection point), matching the ticket's "Done when" list.

## Addendum (CI fix, PR #364)

**Bug:** `test_flag_absent_from_dry_run_leaves_ladder_off` invoked
`socr process <pdf> --dry-run` through Click's `CliRunner` with zero
patching. `--dry-run` is checked only inside `process_batch`
(`orchestrator.py:709`) — the single-file `process` command never reads
`config.dry_run` at all, so the flag is silently inert there and the
invocation ran the real agentic pipeline. Locally (ollama installed) that
happened to produce output and pass; in CI (no provider) engine
auto-selection fell through to `gemini`, which then failed with "page(s) 1
produced no usable output" — the exact CLAUDE.md no-provider trap, and also
a vacuous test: passing proved nothing about `table_judge_ladder` because the
test asserted only `exit_code == 0`.

**Fix:** replaced it with two tests that patch
`socr.pipeline.orchestrator.UnifiedPipeline` at its constructor (the same
seam `test_gh190_empty_table_surfacing.py` uses for hermetic `process()` CLI
tests) with a stub that never touches an engine, a judge, or the network, and
`--primary qwen` to also skip `resolve_auto_engine()`. Both assert the actual
promise — the config `UnifiedPipeline` was constructed with has
`table_judge_ladder is False` — not just exit code:

- `test_flag_absent_leaves_ladder_off_end_to_end` — ambient environment.
- `test_flag_absent_leaves_ladder_off_with_ollama_unreachable` — same
  invocation with `OLLAMA_HOST` pointed at a dead port
  (`http://127.0.0.1:1`), proving nothing on this path makes a live call.

Both pass in this environment (ollama present) and with `OLLAMA_HOST`
redirected to a dead port; the whole file dropped from ~56s (real PDF
processing) to ~0.2s, corroborating that the old test was doing real work it
had no business doing.

**Files:** `tests/test_ladder_config.py` only — no change to
`src/socr/core/config.py` or `src/socr/cli.py`.

**Tests:**
`~/venvs/socr/bin/pytest tests/test_ladder_config.py -q` → 13 passed (both
ambient and with `OLLAMA_HOST=http://127.0.0.1:1`).
`~/venvs/socr/bin/pytest tests/test_gh190_empty_table_surfacing.py tests/test_r174b_cli_guards.py tests/test_config_from_file.py tests/test_cli_flag_agentic_status_gh142.py -q`
→ 81 passed.
`uvx ruff@0.16.0 format --check .` (whole repo) → 422 files already
formatted, clean.

Note: since G1 landed, the initiative progressed substantially on this
branch (A3 changed `table_judge_rung2_binary`'s default from `gemini` to
`agy` — the free-tier gemini CLI headless auth was found dead — through H1);
this fix only touches the CLI-invocation test and does not reopen any of
that.
